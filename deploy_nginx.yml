---
- name: Deploy Nginx Pod
  hosts: localhost
  become: yes
  tasks:
    - name: Create Nginx Pod
      k8s:
        api_version: v1
        kind: Pod
        namespace: default
        name: nginx-pod
        spec:
          containers:
            - name: nginx
              image: nginx
              ports:
                - containerPort: 80

