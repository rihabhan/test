---
- hosts: localhost
  become: yes
  collections:
    - kubernetes.core
  tasks:
    - name: Create a pod
      k8s:
        state: present
        definition:
          apiVersion: v1
          kind: Pod
          metadata:
            name: "nginx-pod"
            namespace: default
          spec:
            containers:
              - name: nginx-container
                image: nginx



