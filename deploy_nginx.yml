- hosts: localhost
  collections:
    - kubernetes.core
  tasks:
    tasks:
    - name: Install kubernetes library
      pip:
        name: kubernetes
        state: present
    - name: Create a pod
      k8s:
        state: present
        definition:
          apiVersion: v1
          kind: Pod
          metadata:
            name: "nginx-pod2"
            namespace: default
          spec:
            containers:
              - name: nginx-container
                image: nginx

